export interface Member {
  name: string;
  season: string;
  color: string;
  bgColor: string;
}

export interface Season {
  season: string;
  title: string;
  description: string;
  color: string;
  bgColor: string;
}
